<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - MotionMania</title>
    <link href="https://fonts.googleapis.com/css?family=Alegreya+Sans+SC|Roboto+Condensed" rel="stylesheet">   
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="#">
                    <span>
                        @Html.ActionLink("MotionMania", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    </span>
                </a>
            </div>
            <div class="navbar-collapse collapse">
                <ul id="navbarOp" class="nav navbar-nav">
                    @if (User.Identity.IsAuthenticated)
                    {
                    }
                </ul>
                <div id="loginBtn" style="color:#C0C0C0">
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div>
    </div>
    <div class=" body-content">
        @RenderBody()
        <hr style="border-color:#34597F;" />
        <footer id="copyInfo" class="text-center">
            <p>&copy; @DateTime.Now.Year - Motion Maniacs Inc</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
